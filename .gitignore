# Segurança: o .env real nunca entra no git — versione só o .env.example.
.env
.env.*
!.env.example

# Sistema operacional
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/

# Claude Code — estado local, não versiona
.claude/settings.local.json

# Node — dependências e build
node_modules/
dist/
coverage/

# SQLite — banco local (WAL) não versiona; só a estrutura via migrações
*.db
*.db-wal
*.db-shm
